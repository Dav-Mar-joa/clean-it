doctype html
html(lang="fr")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Checklist
    link(rel="stylesheet", href="/css/style.css")
    link(rel="manifest", href="/manifest.json")
    script(src="/js/app.js")
  body
    h1 🧹 Checklist de l'appartement

    form(method="post" action="/reset")
      button(type="submit") 🔄 Réinitialiser tout

    ul
      each item in checklist
        li
          form(method="post", action=`/toggle/${item._id}`)
            label(style=`text-decoration: ${item.done ? 'line-through' : 'none'};`)
              input(type="checkbox", checked=item.done, onchange="this.form.submit()")
              | #{item.texte}

    br
    a(href="/autres") ➕ Aller aux autres onglets